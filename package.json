{
  "name": "gamleetee-tunnel-chat",
  "version": "0.1.0",
  "description": "Ephemeral end-to-end encrypted chat and file tunnel for two people.",
  "type": "module",
  "private": false,
  "license": "MIT",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node server/index.js",
    "dev": "node --watch server/index.js",
    "test": "node --test",
    "check": "node --check server/index.js && node --check server/room-registry.js && node --check server/websocket-peer.js && node --check public/app.js && node --check public/crypto.js"
  }
}
